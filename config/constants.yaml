# Space1 Constants Configuration
# All numerical constants for the framework

success_rate_base: 0.3
throughput_base: 1.0
cost_base: 0.0
time_base: 1.0
price_per_task: 50.0
learning_rate: 0.01
knowledge_max: 1.0
initial_knowledge: 0.0
skill_max: 1.0
initial_skill: 0.0
ema_default_alpha: 0.2
homeostasis_target: 1.0
rating_min: 1.0
rating_max: 5.0

# Phase 2 additions
phi_cap: 1000.0          # Cap for normalized phi
psi_max: 1.0             # Maximum risk
upsilon_cap: 1.0         # Soft cap for reputation
upsilon_decay: 0.95      # EMA decay factor for reputation
pid_kp: 1.0              # PID proportional gain
pid_ki: 0.1              # PID integral gain
pid_kd: 0.05             # PID derivative gain
xi_alpha: 0.7            # Ξ quality weight
xi_beta: 0.3             # Ξ opportunity weight
phi_r_lambda_psi: 0.5    # Φ_R risk penalty weight
phi_r_alpha_rep: 0.3     # Φ_R reputation amplification
