# 02_BOUNDARIES_AND_APPLICATION.md — Границы, единицы и условия применения формул ядра

> **Статус:** ✅ Каноническое **дополнение** к `02_MATHEMATICAL_CORE.md`
> **Версия:** 1.0
> **Дата:** 2026-08-01
> **Назначение:** закрыть неоднозначности, из-за которых реализация неизбежно «плывёт».  
> Источник вопросов — фактический код (`srs/utility/*`, `srs/orchestrator/core.py`): где код сделан «абы как», в спеке не хватало запрета или единицы измерения.
> **Правило:** при конфликте кода с этим документом правится **код**, не спецификация. При конфликте с `02` — правится этот документ только вместе с явным changelog в `02`.

---

## 0. Принцип

Если формула допускает две интерпретации (единицы, масштаб, что подставлять в гейт), реализация выберет удобную, а не верную. Ниже — **единственные** допустимые интерпретации.

---

## 1. Единицы и масштаб (запрет смешения)

| Величина | Единица / масштаб | Запрещено |
|----------|-------------------|-----------|
| $\Phi$, $\hat\Phi$ (в $U$ и $\mathcal D$) | **\$/час** (может быть < 0) | Нормировать в [0,1] **до** решения $\mathcal D$ / $U$ и терять знак убытка |
| $\Psi$, $\psi_{max}$ | **\$** (ожидаемые потери) | Возвращать $\Psi\in[0,1]$ «как риск-скор» и сравнивать с $\psi_{max}$ в \$ |
| $Q$, $\hat Q$, $q_{min}$ | **[0, 1]** | Путать со шкалой Verifier 0–10 |
| $\Upsilon_k$, $\Upsilon_{scalar}$, $\Omega$, $\text{Trust}$ | **[0, 1]** | |
| $\Gamma_{hard}$ | $\{0,-\infty\}$ | Любое конечное «почти veto» |
| $\Gamma_{soft}$ | **[0, +\infty)**; в $U$ используется $\min(1,\Gamma_{soft})$ | Считать $\Gamma_{soft}$ уже «штрафом к score» и ещё раз вычитать $\Psi$ |
| $H$ | без верхней крышки (может быть > 1) | Считать LOW недостижимым из-за `min(1, ratio)` |
| $C_t$ (в $\mathcal D$) | **доля свободного контекста ∈ [0,1]** (`02` §IV.15) | Подставлять `balance` (деньги) |
| Денежный остаток | отдельный гейт, **не** $C_t$ | |

**Инвариант:** сравнение в гейтах только в **одинаковых единицах**.  
`if psi > psi_max` допустимо только если оба в \$.

---

## 2. $\Phi$ — когда что входит

### 2.1 Обязательные термы при оценке задачи (pre-decision)

$$
R_{adj} = P\cdot(1+b_Q(Q))\cdot\rho_{risk},\quad
\Phi = \frac{R_{adj}-C}{\max(\varepsilon_T,T)}
$$

- $\rho_{risk}=\prod_k(1-P_k w_k)$ — **обязателен**, если есть оценка $P_k$ (иначе $\rho_{risk}=1$ с явной пометкой «risk prior absent» в логе).
- $C$ включает $C_{tokens}+C_{platform}+C_{processing}$ (аксиома A3), не только «базовую стоимость».
- $\varepsilon_T>0$ (реестр Φ-1); **запрещено** деление на сырое $T$ без нижней границы.

### 2.2 Запрещённые упрощения в $\mathcal D(T)$ / $U$

- Зажимать $\hat\Phi$ в $[0,1]$ **вместо** нормирования через отдельный $\hat\Phi_{norm}$ для весов $U$.  
  Допустимо: хранить $\Phi$ в \$/час; в $U$ использовать **явную** нормировку (запись U-1 в `02`), не уничтожая исходное $\Phi$ для логов и $\rho_{risk}$.
- Считать «прибыль» = $P$ без $T$ и без $C$.

### 2.3 $r_{min}$ (минимальная эффективная ставка)

Гейт $r_{eff}\ge r_{min}$ — **до** EXECUTE, независимо от $U>0$.  
Если $r_{min}$ не задан в конфиге — задача **не** EXECUTE по умолчанию «как будто порога нет»: либо DECLINE, либо CLARIFY владельцу системы (конфиг), но не молчаливый EXECUTE.

---

## 3. $\Psi$ — одна семантика

### 3.1 Канон

$$\Psi = P_{fail}\cdot(C_{direct}+C_{reputation})\quad[\$]$$

Единственный допустимый вход в $\mathcal D(T)$ и сравнение с $\psi_{max}$.

### 3.2 Запрещено

- «Альтернативный» путь, возвращающий взвешенную сумму факторов в $[0,1]$ и передающий её в `evaluate_decision_rule` как $\Psi$.
- Смешивать `risk_premium` (безразмерная надбавка к $P_{fail}$) с итоговым $\Psi$ без умножения на стоимость последствий.

### 3.3 `risk_premium` (таблица флагов `02` §IV.4)

- Влияет на $P_{fail}$ (или на $\rho_{risk}$ в $\Phi$), **не** заменяет $\Psi$-гейт.
- Потолок $\min(0.5,\sum w_k\mathbb 1_k)$ — обязателен.

---

## 4. $U$ — полный состав, не «только прибыль»

### 4.1 Канон (`02` §IV.11)

$$
U_{raw}=\lambda_\Phi\hat\Phi_{norm}+\lambda_\Upsilon\Upsilon_{scalar}+\lambda_\Omega\Omega+\lambda_Q\hat Q,\quad
U=U_{raw}\cdot(1-\min(1,\Gamma_{soft}))
$$

$\sum\lambda=1$, $\lambda$ — **только** из Mission.calibrate (или конфиг, эквивалентный таблице Mission), не ad-hoc.

### 4.2 Запрещено

- $U := \hat\Phi\cdot(1-\min(1,\Gamma_{soft}))$ как полная реализация §IV.11 (это **урезанный** fallback только если явно включён флаг `U_mode=phi_only` в конфиге MVP и залогирован).
- Вычитать $\Psi$ из $U$ (гейт $\Psi\le\psi_{max}$ уже в $\mathcal D$).
- Подставлять ненормированный $\Phi$ в \$/час в сумму с $\Upsilon\in[0,1]$ без нормировки.

### 4.3 Fallback MVP (если разрешён конфигом)

Если `U_mode=phi_only`: в логе обязательно `U_mode=phi_only`; $\mathcal D$ всё равно применяет полные гейты $\Gamma,\Psi,C_t,H_{TZ},q_{min}$.

---

## 5. Два денежных/ресурсных гейта — не путать с $C_t$

### 5.1 Вычислительный $C_t$ (`02` §IV.15)

- $C_t\in[0,1]$ — свободная доля контекста/compute-бюджета.
- В $\mathcal D$: DECLINE если $C_t < C_{min}^{comp}$ (порог **безразмерный**).

### 5.2 Денежный остаток

- Отдельное условие: `balance` (или $B_{remaining}$) $\ge B_{min}$ перед EXECUTE, если задача требует предоплаты/резерва.
- **Запрещено** передавать `balance` в параметр, названный $C_t$, в `evaluate_decision_rule`.

### 5.3 Минимальная спецификация входов $\mathcal D$

| Параметр | Смысл |
|----------|--------|
| `C_t` | compute/context free ratio |
| `C_min` | порог для compute |
| `B` / `balance` | деньги (отдельный аргумент или отдельный гейт) |
| `B_min` | денежный порог |

Реализация, склеивающая их в один аргумент, **не соответствует** канону.

---

## 6. $\Gamma_{soft}$ — как считать violation

### 6.1 Канон

$$\Gamma_{soft}=\sum_s w_s\cdot\text{violation}_s,\quad \text{violation}_s\in[0,1],\ w_s\ge 0$$

- `violation_s=0` — правило соблюдено; `1` — полное нарушение.
- Бинарное soft-правило: violation ∈ {0,1}.

### 6.2 Запрещено

- При нарушении просто делать `penalty += weight` **без** понятия violation ∈ [0,1] (тогда несколько soft-правил с weight=1 дают $\Gamma_{soft}>1$ и обнуляют $U$ без градуировки).
- Игнорировать $w_s$ из приоритета принципа (Mission-3).

---

## 7. $\mathcal D(T)$ — порядок и семантика исходов

Порядок **фиксирован** (`02` §IV.14):

1. REJECT ← только $\Gamma_{hard}=-\infty$
2. DECLINE ← $\Psi>\psi_{max}$ **или** compute-гейт **или** (отдельно) денежный гейт
3. CLARIFY ← $H_{TZ}$, VoI, $H$
4. EXECUTE ← $U>0$ и $\hat Q\ge q_{min}$ (и $r_{min}$, если задан)
5. иначе DECLINE

### 7.1 REJECT vs DECLINE

- REJECT: повтор с другой ценой **не** помогает (комплаенс).
- DECLINE: можно пересмотреть условия / состояние агента.

### 7.2 VoI

- Канон: $\mathbb E[\Phi\mid\text{info}]-\mathbb E[\Phi\mid\text{no info}]$ в \$/час.
- Запрещено как единственная формула: `VoI = Φ * H_TZ * 0.2` без объявления эвристики в конфиге (`VoI_mode=heuristic_v0`).

### 7.3 Пороги по умолчанию (пока [О])

| Параметр | Значение в `02` | Несогласованность, которую нельзя оставлять «как в коде» |
|----------|-----------------|-----------------------------------------------------------|
| $H_{clarify}$ | **0.8** (граница NORMAL/HIGH) | Код с 0.5 — отклонение от канона |
| $H_{TZ,max}$ | реестр D-2 | Должен быть в конфиге, не «магическое» 1.0 без комментария |

---

## 8. Mission — допустимые имена и веса

### 8.1 Типы миссии (закрытый список)

`SURVIVAL | GROWTH | MAINTENANCE | MAXIMIZE | PREMIUM | CHARITY`

- **Нет** типа `BALANCED`.  
- Если в API пришло неизвестное имя → маппинг в `MAINTENANCE` (или `SURVIVAL` на MVP) **и** warning в лог; не тихий no-op.

### 8.2 Веса $\lambda$

- Источник истины: таблица Mission + state modifiers (`02` §IV.12).
- PID / «pressures» (`HomeostaticRegulator.get_weight_adjustment`) — **Пост-MVP** альтернатива modifiers, не параллельный источник без флага.

---

## 9. Качество: две шкалы

| Слой | Шкала | Где |
|------|-------|-----|
| $Q$, $\hat Q$, $q_{min}$ | [0, 1] | ядро, $\mathcal D$ |
| Verifier critic scores | [0, 10], accept ≥ 8.5 | `03` §VIII.6 |

**Правило перевода (обязательно, если Verifier питает $Q$):**  
$Q_{from\_verifier} = \text{clamp}(\min_k score_k / 10,\ 0,\ 1)$  
либо явная калибровочная кривая в конфиге — но не сравнение 8.5 с $q_{min}=0.7` напрямую.

---

## 10. $H$ — границы ratio

| Ratio | Верхний срез |
|-------|----------------|
| `balance_ratio` | да, по кусочной формуле `02` §IV.7 (≤ 1 при $balance\ge budget_{target}$) |
| `rating_ratio`, `quality_ratio` | **нет** — иначе LOW ($H>1.2$) недостижим |
| `workload_ratio` | может быть > 1 при перегрузе |

---

## 11. Прогноз $\hat\Phi,\hat Q,\hat\Psi$ (стык с `03`)

- Ансамбль §III.1 пайплайна выдаёт величины **в тех же единицах**, что канон §1 этого документа.
- Запрещено: `phi_hat = clamp(ensemble, 0, 1)` как финальный $\hat\Phi$ для $U$ без отдельного norm-шага.
- $\hat\Psi$ — в \$, сопоставим с $\psi_{max}$.

---

## 12. Очередь и source (ссылка, не дублирование)

Полный контракт: `01_CONCEPT.md` §I.3–I.5 и `03_STAGE0_ADMISSION.md`.  
Здесь только инвариант для математики решения:  
**`source` и `emergency_override` не меняют формулы $\Gamma,U,\Psi,\mathcal D$** — только admission.

---

## 13. Минимальный чеклист для реализации (acceptance)

Реализация соответствует канону, только если:

1. $\Psi$ и $\psi_{max}$ в \$; нет path с $\Psi\in[0,1]$ в $\mathcal D$.
2. $C_t$ ≠ `balance`; денежный гейт отделён.
3. $U$ либо полная 4-веса, либо явный `U_mode=phi_only`.
4. $\hat\Phi$ не зажат в [0,1] с потерей \$/час для учёта.
5. $\rho_{risk}$ учтён или явно `rho=1` + лог.
6. $H_{clarify}=0.8$ (или конфиг = канон), не «другое число без записи в реестре».
7. Mission ∈ закрытого списка §8.1.
8. Verifier 0–10 не сравнивается с $q_{min}$ без масштабирования.
9. $\Gamma_{soft}$ через $w_s\cdot\text{violation}_s$.
10. `source` / emergency не отключают $\Gamma$.

---

## 14. Происхождение пунктов (trace to code smells)

| § | Что в коде спровоцировало |
|---|---------------------------|
| 1, 5 | `C_t=self.core_agent.metrics.balance` |
| 2, 11 | `phi_hat = max(0, min(1, phi_hat))` |
| 3 | `compute_psi` non-canonical → [0,1] |
| 4 | `u_val = phi_hat * (1 - min(1, gamma_soft))` без Υ,Ω,Q |
| 4 | module docstring `score − Ψ` |
| 6 | `penalty += weight` без violation∈[0,1] |
| 7 | `VoI = phi_hat * h_tz * 0.2`; `h_clarify=0.5` |
| 8 | `mission_profile="BALANCED"` |
| 9 | Verifier 8.5 vs Q∈[0,1] |
| 2 | отсутствие $\rho_{risk}$ в `compute_phi` |

---

*Документ создан: 2026-08-01*
*Версия: 1.0*
*Связан с: `02_MATHEMATICAL_CORE.md`, `01_CONCEPT.md` §I.5, `03_STAGE0_ADMISSION.md`*
