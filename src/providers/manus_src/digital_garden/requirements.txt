# 🌱 Цифровой Сад — Зависимости
# Минимальный набор для Management Core

# HTTP-клиент (для API-вызовов и сервера)
aiohttp>=3.9.0

# Конфигурация
pyyaml>=6.0

# Память (встроенная, без внешних баз)
# Для продвинутого режима (опционально):
# chromadb>=0.4.0       # Векторная память
# networkx>=3.0        # Граф знаний
# sentence-transformers>=2.0  # Эмбеддинги (требует GPU или CPU-модель)

# Для Telegram-бота (опционально):
# python-telegram-bot>=20.0
