# srs/monitoring — trial (not wired to orchestrator)

SystemMonitor + bridge to `srs/homeostasis`.
Canon: `docs/drafts/MONITORING.md`.

```bash
cd srs/monitoring && python test_integration.py
```

All 13 integration checks must pass.
