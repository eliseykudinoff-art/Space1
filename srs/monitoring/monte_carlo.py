# See repo artifacts / run locally — full script generates figs + stats.
# Entry: python monte_carlo.py  (N=400, T=100)
print("Use full monte_carlo from workspace artifacts/srs_monitoring/monte_carlo.py")
